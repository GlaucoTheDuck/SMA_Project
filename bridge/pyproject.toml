[project]
name = "bridge"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.38.0",
    "shared",
    "spade>=4.1.4",
]

[tool.uv.sources]
shared = { path = "../shared", editable = true }
